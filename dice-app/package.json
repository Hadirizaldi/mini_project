{
  "name": "dice-app",
  "version": "1.0.0",
  "description": "a dice program that generates random results for two players",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i ./src/assets/css/main.css -o ./src/output.css --watch" 
  },
  "author": "hadirizaldi",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
